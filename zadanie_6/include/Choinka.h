#include<iostream>

class Choinka{
    int rows, start, limiter;
    public:
        Choinka(int rows, int start, int limiter);
        void wyswietl_plat();
        void wyswietl_stopke(int liczba);
};